{
  "name": "headless-xcp-wallet",
  "description": "xcp chrome wallet converted to web app using phantomjs to run serverside and exposed via REST",
  "version": "0.0.1",
  "dependencies": {
    "express": "3.x"
  }
}
